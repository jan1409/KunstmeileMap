# Kunstmeile Web App (KunstmeileMap)

Phase 1 implementation. Internal planning docs live in a private Obsidian vault (not in this repo).

## Local dev

1. `cp .env.example .env.local` and fill in real Supabase values from the project dashboard.
2. `pnpm install`
3. `pnpm dev` -> http://localhost:5173

## Tech stack

React + TypeScript + Vite + Tailwind + Three.js + Spark + Supabase.
